import './globals.css';
export const metadata = { title: 'Enterprise Leave System' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="zh-TW"><body>{children}</body></html>;
}
